
  # Create Clickable Sandbox Prototype

  This is a code bundle for Create Clickable Sandbox Prototype. The original project is available at https://www.figma.com/design/dBU0oHjVj3Zur4FiNH1YpM/Create-Clickable-Sandbox-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  